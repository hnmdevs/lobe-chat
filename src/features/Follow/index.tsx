'use client';

import { memo } from 'react';

const Follow = memo(() => {
  return null; // Hidden as requested - social media links removed from UI
});

Follow.displayName = 'Follow';

export default Follow;
